<div class="mentor-card-container large mb-5">
  <div class="mentor-card">
    <div class="mentor-rank">
      <span>#</span>
      <p><%= index + 1 %></p>
    </div>
    <%= cl_image_tag match[0].mentor.photo %>
    <h6><%= match[0].mentor.first_name %> <%= match[0].mentor.last_name %></h6>
    <p><%= match[0].mentor.profile.current_title %> at <%= match[0].mentor.profile.current_employer %></p>
    <p><%= match[0].mentor.profile.city %>, <%= match[0].mentor.profile.state %></p>
  </div>
  <div class="mentor-tagline">
    <p>"<%= match[0].mentor.profile.tagline %>"</p>
  </div>
  <div class="percent-match">
    <p><%= match[1] %> % match!</p>
  </div>
  <div class="match-button">
    <button type="button" class="btn-ghost small" data-toggle="modal" data-target="#mentor-<%= match[0].mentor.id %>">View profile</button>
    <%= button_to "Match with #{match[0].mentor.first_name}", matches_path, class: "btn-solid small", params: { match: { mentor_id: match[0].mentor.id } } %>
  </div>
</div>



<!-- Button trigger modal -->

<!-- Modal -->
<div class="modal fade" id="mentor-<%= match[0].mentor.id %>" tabindex="-1" role="dialog" aria-labelledby="exampleModalScrollableTitle" aria-hidden="true">
  <div class="modal-dialog modal-xl modal-dialog-scrollable" role="document">
    <div class="modal-content">
      <div class="modal-header">
        <h5 class="modal-title" id="exampleModalScrollableTitle"></h5>
        <button type="button" class="close" data-dismiss="modal" aria-label="Close">
          <span aria-hidden="true">&times;</span>
        </button>
      </div>
      <div class="modal-body modal-background">
        <div class="small-container">
          <%= render "profiles/profile-modal", profile: match[0].mentor.profile %>
        </div>
      </div>
      <div class="modal-footer">
        <button type="button" class="btn-ghost small" data-dismiss="modal">Close</button>
        <%= button_to "Match with #{match[0].mentor.first_name}", matches_path, class: "btn-solid small", params: { match: { mentor_id: match[0].mentor.id } } %>
      </div>
    </div>
  </div>
</div>
